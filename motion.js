const topbar=document.querySelector('.topbar');
addEventListener('scroll',()=>topbar.classList.toggle('scrolled',scrollY>20),{passive:true});
const hero=document.querySelector('.hero');hero.addEventListener('pointermove',event=>{const box=hero.getBoundingClientRect();hero.style.setProperty('--mouse-x',`${((event.clientX-box.left)/box.width)*100}%`);hero.style.setProperty('--mouse-y',`${((event.clientY-box.top)/box.height)*100}%`)});
const motionTargets=document.querySelectorAll('.content-section,.contact-section,footer');motionTargets.forEach(target=>target.classList.add('reveal-section'));
const motionObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');motionObserver.unobserve(entry.target)}}),{threshold:.13});motionTargets.forEach(target=>motionObserver.observe(target));
document.querySelectorAll('.stats article,.unit-grid a,.achievement-grid article').forEach(card=>card.addEventListener('pointermove',event=>{if(innerWidth<800)return;const rect=card.getBoundingClientRect(),x=(event.clientX-rect.left)/rect.width-.5,y=(event.clientY-rect.top)/rect.height-.5;card.style.transform=`perspective(700px) rotateX(${-y*5}deg) rotateY(${x*5}deg) translateY(-7px)`}));document.querySelectorAll('.stats article,.unit-grid a,.achievement-grid article').forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));
