const soundButton=document.querySelector('#sound');let audioContext,musicTimer,musicPlaying=false;const notes=[146.83,174.61,220,196,174.61,146.83,130.81,164.81];function playTone(frequency,time,duration,volume){const oscillator=audioContext.createOscillator(),gain=audioContext.createGain();oscillator.type='sine';oscillator.frequency.setValueAtTime(frequency,time);gain.gain.setValueAtTime(.0001,time);gain.gain.exponentialRampToValueAtTime(volume,time+.035);gain.gain.exponentialRampToValueAtTime(.0001,time+duration);oscillator.connect(gain).connect(audioContext.destination);oscillator.start(time);oscillator.stop(time+duration+.05)}function startMusic(){audioContext??=new(window.AudioContext||window.webkitAudioContext)();audioContext.resume();let index=0;const playSequence=()=>{if(!musicPlaying)return;const now=audioContext.currentTime;for(let step=0;step<8;step++){const note=notes[(index+step)%notes.length];playTone(note,now+step*.42,.52,.045);if(step%2===0)playTone(note/2,now+step*.42,.3,.018)}index=(index+8)%notes.length;musicTimer=setTimeout(playSequence,3360)};musicPlaying=true;playSequence()}function stopMusic(){musicPlaying=false;clearTimeout(musicTimer)}soundButton.addEventListener('click',()=>{soundButton.classList.contains('active')?startMusic():stopMusic()});
